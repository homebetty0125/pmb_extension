const theme = {
    space: (param) => {

        let base = 4;
        return `${base * param}px`;

    },
    palette: {
        font: '#333',
        border: '#d9d9d9',
        disabled: '#f0f0f0',
        container: '#F0F2F5',
        blue: '#1890ff',
    },
};

export default theme;
